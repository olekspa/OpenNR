name: "Nexus: Upload Release"

# `mode=aio` (default) uploads only the AIO to the fork's mod page.
# `mode=matrix` runs the upstream per-feature fan-out (`feature_version_audit.py` builds the matrix).
# Only `prepare-nexus-matrix` branches on mode; downstream jobs are
# matrix-shape-agnostic.

on:
    workflow_dispatch:
        inputs:
            mode:
                description: "Upload mode: 'aio' uploads only the AIO to a single mod page (fork default); 'matrix' fans out per-feature to their respective Nexus pages (upstream behavior)."
                required: false
                type: choice
                default: "aio"
                options:
                    - "aio"
                    - "matrix"
            tag:
                description: "Release tag to upload to Nexus"
                required: true
            nexus_game_id:
                description: "Nexus game ID"
                required: false
                default: "skyrimspecialedition"
            nexus_mod_id:
                description: "Target Nexus mod ID. In `aio` mode this is the fork's single mod page (defaults to 180419 if empty). In `matrix` mode this seeds the CORE row of the upstream matrix (defaults to 86492 if empty)."
                required: false
                default: ""
            artifact_pattern:
                description: "Artifact glob pattern. Defaults: `CommunityShaders_AIO-*.7z` in aio mode, `CommunityShaders-*.7z` (core .7z) in matrix mode."
                required: false
                default: ""
            mod_filename:
    description: "Nexus mod filename. Defaults: `OpenNR` in aio mode, `Community Shaders` in matrix mode."
                required: false
                default: ""
            dry_run:
                description: "If true, do not upload to Nexus; only report the planned upload"
                required: false
                type: choice
                default: "true"
                options:
                    - "true"
                    - "false"
            changelog:
                description: "Optional release notes or changelog for Nexus"
                required: false
    workflow_call:
        inputs:
            mode:
                required: false
                type: string
                default: "aio"
            tag:
                description: "Release tag to upload to Nexus"
                required: true
                type: string
            nexus_game_id:
                required: false
                type: string
                default: "skyrimspecialedition"
            nexus_mod_id:
                required: false
                type: string
                default: ""
            artifact_pattern:
                required: false
                type: string
                default: ""
            mod_filename:
                required: false
                type: string
                default: ""
            dry_run:
                required: false
                type: string
                default: "true"
            changelog:
                required: false
                type: string
                default: ""
        secrets:
            UNEX_NEXUSMODS_SESSION_COOKIE:
                required: false
            UNEX_APIKEY:
                required: false

# Least-privilege for the whole workflow: every job here only reads from
# GitHub (release listing via `gh api`, asset download via `gh release
# download`) and writes only to Nexus via the UNEX_* secrets. No mutation
# of repo state.
permissions:
    contents: read

jobs:
    prepare-nexus-matrix:
        runs-on: ubuntu-latest
        outputs:
            matrix: ${{ steps.generate.outputs.matrix }}
            upload_matrix: ${{ steps.generate.outputs.upload_matrix }}
            tag: ${{ steps.resolve.outputs.tag }}
            version: ${{ steps.resolve.outputs.version }}
            dry_run: ${{ steps.dryrun.outputs.dry_run }}
            has_uploads: ${{ steps.generate.outputs.has_uploads }}
            mode: ${{ steps.resolve.outputs.mode }}
        steps:
            - name: Checkout repository
              uses: actions/checkout@v6
              with:
                  fetch-depth: 0

            - name: Setup Python
              uses: actions/setup-python@v6
              with:
                  python-version: "3.11"

            - name: Resolve release tag and mode
              id: resolve
              run: |
                  TAG="$INPUT_TAG"
                  if [ -z "$TAG" ]; then
                    echo 'No tag provided for Nexus upload' >&2
                    exit 1
                  fi
                  if [[ "$TAG" == *-* ]]; then
                    echo "ERROR: '$TAG' is a pre-release tag. Only stable releases are uploaded to Nexus." >&2
                    exit 1
                  fi
                  DRAFT=$(gh api "repos/$GITHUB_REPOSITORY/releases" \
                    --jq ".[] | select(.tag_name == \"$TAG\") | .draft" 2>/dev/null | head -1)
                  if [ "$DRAFT" != "false" ]; then
                    echo "ERROR: Release '$TAG' is not published. Publish the GitHub release before uploading to Nexus." >&2
                    exit 1
                  fi
                  VERSION="${TAG#v}"
                  MODE="${INPUT_MODE:-aio}"
                  if [[ "$MODE" != "aio" && "$MODE" != "matrix" ]]; then
                    echo "ERROR: mode must be 'aio' or 'matrix' (got '$MODE')" >&2
                    exit 1
                  fi
                  echo "tag=$TAG"        >> $GITHUB_OUTPUT
                  echo "version=$VERSION" >> $GITHUB_OUTPUT
                  echo "mode=$MODE"      >> $GITHUB_OUTPUT
              env:
                  INPUT_TAG: ${{ inputs.tag || '' }}
                  INPUT_MODE: ${{ inputs.mode || '' }}
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - name: Compute dry-run mode
              id: dryrun
              run: |
                  if [ "$INPUT_DRY_RUN" = "false" ]; then
                    echo "dry_run=false" >> $GITHUB_OUTPUT
                  else
                    echo "dry_run=true" >> $GITHUB_OUTPUT
                  fi
              env:
                  INPUT_DRY_RUN: ${{ inputs.dry_run || '' }}

            - name: Generate Nexus upload matrix
              id: generate
              run: |
                  set -euo pipefail
                  MODE='${{ steps.resolve.outputs.mode }}'

                  # Workflow inputs default to empty so matrix mode keeps
                  # its upstream defaults below; aio mode uses fork defaults.
# Keep 180419/"OpenNR" in sync with
                  # .github/configs/project.yaml, which build-fomod-package.py
                  # reads for the same facts -- left as literals here rather
                  # than reading that file too, to avoid touching this
                  # already-working upload pipeline's logic.
                  if [ "$MODE" = "aio" ]; then
                    EFFECTIVE_MOD_ID="${INPUT_NEXUS_MOD_ID:-180419}"
                    EFFECTIVE_ARTIFACT="${INPUT_ARTIFACT_PATTERN:-CommunityShaders_AIO-*.7z}"
EFFECTIVE_FILENAME="${INPUT_MOD_FILENAME:-OpenNR}"
                  else
                    EFFECTIVE_MOD_ID="${INPUT_NEXUS_MOD_ID:-86492}"
                    EFFECTIVE_ARTIFACT="${INPUT_ARTIFACT_PATTERN:-CommunityShaders-*.7z}"
                    EFFECTIVE_FILENAME="${INPUT_MOD_FILENAME:-Community Shaders}"
                  fi
                  export EFFECTIVE_MOD_ID EFFECTIVE_ARTIFACT EFFECTIVE_FILENAME

                  # Fetch the release body once — both modes use it as the
                  # changelog for the CORE/AIO row.
                  gh api "repos/$GITHUB_REPOSITORY/releases/tags/$RELEASE_TAG" \
                    2>/dev/null > release.json || echo '{}' > release.json

                  if [ "$MODE" = "aio" ]; then
                    if [ -z "$EFFECTIVE_MOD_ID" ] && [ "$DRY_RUN" != "true" ]; then
echo "::error::aio mode requires nexus_mod_id for a real upload. Set it via the workflow input or update the default once the OpenNR Nexus page exists."
                      exit 1
                    fi
                    if [ -z "$EFFECTIVE_MOD_ID" ]; then
                      echo "::warning::nexus_mod_id is empty — dry-run continues, but a real upload would fail."
                    fi
                    RELEASE_VER="${RELEASE_TAG#v}"
                    export RELEASE_VER
                    python3 << 'PYEOF'
                  import json, os
                  from nexus_changelog import strip_audit
                  with open('release.json') as f:
                      release = json.load(f)
                  if not isinstance(release, dict):
                      release = {}
                  release_ver = os.environ['RELEASE_VER']
                  # Post this version's release notes (minus the internal audit
                  # block). Re-run duplicates are prevented by the idempotency
                  # filter below, not by reformatting the body (issue #198).
                  changelog = strip_audit(release.get('body') or '')
                  row = {
                      'name': 'core',
                      'is_core': True,
                      'nexus_mod_id': os.environ['EFFECTIVE_MOD_ID'],
                      'artifact_pattern': os.environ['EFFECTIVE_ARTIFACT'],
                      'mod_filename': os.environ['EFFECTIVE_FILENAME'],
                      'auto_upload': True,
                      'changelog': changelog,
                      'file_description': (
 f'OpenNR {release_ver} AIO. See changelog for included features. '
                          'FOMOD installer: pick your runtime\'s prebuilt shader cache during '
                          'setup to skip first-launch shader compilation, or skip that step.'
                      ),
                  }
                  with open('nexus-matrix-raw.json', 'w') as f:
                      json.dump([row], f)
                  PYEOF
                  else
                    # Matrix mode: upstream behavior — the audit tool emits
                    # the full per-feature matrix.
                    ARGS=( --export-nexus-matrix --matrix-output nexus-matrix-raw.json )
                    PREVIOUS_TAG=$(git tag --merged "$RELEASE_TAG" --list 'v*.*.*' --sort=-v:refname 2>/dev/null \
                      | grep -v -- '-' \
                      | awk -v current="$RELEASE_TAG" '$0 != current { print; exit }')
                    if [ -n "$PREVIOUS_TAG" ]; then
                      ARGS+=( --base "$PREVIOUS_TAG" )
                    else
                      ARGS+=( --all-features )
                    fi
                    ARGS+=( --core-mod-id "$EFFECTIVE_MOD_ID" )
                    ARGS+=( --core-filename "$EFFECTIVE_FILENAME" )
                    ARGS+=( --core-artifact-pattern "$EFFECTIVE_ARTIFACT" )
                    RELEASE_VER="${RELEASE_TAG#v}"
                    ARGS+=( --release-version "$RELEASE_VER" )
                    python tools/feature_version_audit.py "${ARGS[@]}"

                    # Splice this version's changelog (audit-stripped body) into the CORE row.
                    python3 << 'PYEOF'
                  import json, os
                  from nexus_changelog import strip_audit
                  with open('release.json') as f:
                      release = json.load(f)
                  if not isinstance(release, dict):
                      release = {}
                  changelog = strip_audit(release.get('body') or '')
                  with open('nexus-matrix-raw.json') as f:
                      data = json.load(f)
                  for row in data:
                      if row.get('name') == 'core':
                          row['changelog'] = changelog
                          break
                  with open('nexus-matrix-raw.json', 'w') as f:
                      json.dump(data, f)
                  PYEOF
                  fi

                  # Common post-processing: build the upload-eligible matrix
                  # (filter on auto_upload + artifact presence in the release).
                  # Single-row AIO matrix trivially passes this filter.
                  python3 << 'PYEOF'
                  import json, os, fnmatch
                  with open('nexus-matrix-raw.json') as f:
                      data = json.load(f)
                  try:
                      with open('release.json') as f:
                          release = json.load(f)
                      if not isinstance(release, dict):
                          release = {}
                  except Exception:
                      release = {}
                  asset_names = [a['name'] for a in release.get('assets', [])]

                  # Idempotent re-posting (skip a changelog already on Nexus) is handled
                  # by the reusable workflow itself via check_existing (always passed
                  # true below) -- see alandtse/Nexus-Workflows#10, which checks
                  # changelog_html directly instead of mere file presence. Duplicating
                  # that check here would just be a second copy of the same rule that
                  # can drift out of sync with the one nexus-workflows actually runs.
                  def artifact_in_release(row):
                      pat = row.get('artifact_pattern', '')
                      return bool(pat) and any(fnmatch.fnmatch(n, pat) for n in asset_names)
                  upload_data = [row for row in data
                                 if row.get('auto_upload', True) and artifact_in_release(row)]
                  missing = [row for row in data
                             if row.get('auto_upload') is True
                             and not row.get('is_core')
                             and not artifact_in_release(row)]
                  if missing:
                      summary_path = os.environ.get('GITHUB_STEP_SUMMARY')
                      lines = ['## ⚠️ Auto-upload features missing artifacts', '',
                               '| Feature | Pattern | Mod ID |',
                               '|---------|---------|--------|']
                      for row in missing:
                          name = row.get('name', '?')
                          pat = row.get('artifact_pattern', '?')
                          mod_id = row.get('nexus_mod_id', '?')
                          lines.append(f'| `{name}` | `{pat}` | {mod_id} |')
                          print(f'::warning title=Nexus auto-upload missing artifact::'
                                f'{name} (mod {mod_id}) marked autoupload=true but no '
                                f'release asset matches pattern {pat!r}. Likely cause: '
                                f'folder name differs from nexusfilename — set '
                                f'nexusartifactpattern explicitly in the feature .ini.')
                      lines += ['',
                                'These features are marked `autoupload = true` in their '
                                '`.ini` but no release asset matched their derived '
                                'artifact pattern. They will be skipped from this Nexus '
                                'upload. Add `nexusartifactpattern = <Folder.Name-*.7z>` '
                                'to the feature .ini to override the default derivation.']
                      if summary_path:
                          with open(summary_path, 'a') as f:
                              f.write('\n'.join(lines) + '\n')
                  has_uploads = bool(upload_data)
                  with open('nexus-matrix.json', 'w') as f:
                      json.dump({'include': data}, f)
                  with open('nexus-upload-matrix.json', 'w') as f:
                      json.dump({'include': upload_data or [{'name': '_empty', 'skip': True}]}, f)
                  with open('nexus-upload-state.json', 'w') as f:
                      json.dump({'has_uploads': has_uploads}, f)
                  PYEOF

                  DELIM=$(openssl rand -hex 8)
                  {
                    printf 'matrix<<%s\n' "$DELIM"
                    cat nexus-matrix.json
                    printf '\n%s\n' "$DELIM"
                    printf 'upload_matrix<<%s\n' "$DELIM"
                    cat nexus-upload-matrix.json
                    printf '\n%s\n' "$DELIM"
                  } >> "$GITHUB_OUTPUT"
                  HAS_UPLOADS=$(python3 -c 'import json; d=json.load(open("nexus-upload-state.json")); print("true" if d.get("has_uploads") else "false")')
                  echo "has_uploads=${HAS_UPLOADS}" >> "$GITHUB_OUTPUT"
              env:
                  RELEASE_TAG: ${{ steps.resolve.outputs.tag }}
                  INPUT_NEXUS_MOD_ID: ${{ inputs.nexus_mod_id || '' }}
                  INPUT_MOD_FILENAME: ${{ inputs.mod_filename || '' }}
                  INPUT_ARTIFACT_PATTERN: ${{ inputs.artifact_pattern || '' }}
                  DRY_RUN: ${{ steps.dryrun.outputs.dry_run }}
                  VERSION: ${{ steps.resolve.outputs.version }}
                  MODE: ${{ steps.resolve.outputs.mode }}
                  NEXUS_GAME_ID: ${{ inputs.nexus_game_id || 'skyrimspecialedition' }}
                  # Idempotency query against Nexus (blank changelog for versions
                  # already present). Absent key -> changelog still posts.
                  UNEX_APIKEY: ${{ secrets.UNEX_APIKEY }}
                  # The heredocs import .github/scripts/nexus_changelog.py.
                  PYTHONPATH: ${{ github.workspace }}/.github/scripts
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    prepare-artifacts:
        needs: prepare-nexus-matrix
        runs-on: ubuntu-latest
        strategy:
            matrix: ${{ fromJson(needs.prepare-nexus-matrix.outputs.matrix) }}
            fail-fast: false
        steps:
            - name: Download GitHub release assets
              run: |
                  mkdir -p release-assets
                  gh release download "${{ needs.prepare-nexus-matrix.outputs.tag }}" --repo "$GITHUB_REPOSITORY" --pattern "*.7z" --dir release-assets
                  ls -lah release-assets
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - name: Select release artifact
              id: artifact
              continue-on-error: true
              run: |
                  pattern='${{ matrix.artifact_pattern }}'
                  echo "Searching for artifact matching: $pattern"
                  asset=$(find release-assets -maxdepth 1 -name "$pattern" | sort | head -n 1)
                  if [ -z "$asset" ]; then
                    echo "ERROR: No release artifact matching pattern '$pattern' found for ${{ matrix.name }}" >&2
                    echo "Available artifacts:"
                    ls -1 release-assets/ | sed 's/^/  /'
                    echo "artifact_found=false" >> $GITHUB_OUTPUT
                    exit 0
                  fi
                  echo "Found artifact: $asset"
                  echo "artifact_path=$asset" >> $GITHUB_OUTPUT
                  echo "artifact_found=true" >> $GITHUB_OUTPUT

            - name: Upload selected artifact as workflow artifact
              if: steps.artifact.outputs.artifact_found == 'true'
              uses: actions/upload-artifact@v7
              with:
                  name: nexus-upload-${{ matrix.name }}
                  path: ${{ steps.artifact.outputs.artifact_path }}
                  retention-days: 7

            - name: Report planned Nexus upload
              run: |
                  DRY_RUN="${{ needs.prepare-nexus-matrix.outputs.dry_run }}"
                  FOUND="${{ steps.artifact.outputs.artifact_found }}"
                  if [ "$FOUND" != "true" ]; then
                    echo "SKIPPED: No artifact found for ${{ matrix.name }}"
                    echo "Pattern: ${{ matrix.artifact_pattern }}"
                  else
                    echo "Planned Nexus upload:"
                    echo "  Nexus mod ID: ${{ matrix.nexus_mod_id }}"
                    echo "  Artifact name: nexus-upload-${{ matrix.name }}"
                    echo "  Release tag: ${{ needs.prepare-nexus-matrix.outputs.tag }}"
                    echo "  Version: ${{ matrix.mod_version || needs.prepare-nexus-matrix.outputs.version }}"
                    echo "  Mod filename: ${{ matrix.mod_filename }}"
                    echo "  Auto upload: ${{ matrix.auto_upload }}"
                    if [ "$DRY_RUN" = "true" ]; then
                      echo "Dry run enabled: skipping actual Nexus upload."
                    fi
                  fi

    check-nexus-versions:
        name: Dry Run — Check Nexus Versions
        needs: prepare-nexus-matrix
        if: >
            needs.prepare-nexus-matrix.outputs.dry_run == 'true' &&
            needs.prepare-nexus-matrix.outputs.has_uploads == 'true'
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v6
              with:
                  persist-credentials: false

            - name: Query Nexus for existing versions
              run: |
                  python3 << 'PYEOF'
                  import json, os
                  from nexus_changelog import nexus_versions

                  rows = json.loads(os.environ['UPLOAD_MATRIX']).get('include', [])
                  rows = [r for r in rows if not r.get('skip')]
                  version = os.environ.get('VERSION', '')
                  game_id = os.environ.get('NEXUS_GAME_ID', 'skyrimspecialedition')
                  api_key = os.environ.get('UNEX_APIKEY', '')
                  ua = 'open-shaders-ci/1.0' if os.environ.get('MODE') == 'aio' else 'community-shaders-ci/1.0'
                  summary = open(os.environ['GITHUB_STEP_SUMMARY'], 'a')

                  def w(line=''):
                      print(line)
                      summary.write(line + '\n')

                  w('## Dry Run — Nexus Version Check')
                  w()
                  if not api_key:
                      w('> **Warning:** UNEX_APIKEY is not set — cannot query Nexus.')
                      w()
                  w('| Feature | Planned | Status | Versions on Nexus |')
                  w('|---------|---------|--------|-------------------|')

                  api_ok = False
                  for row in rows:
                      name = row.get('name', '')
                      mod_id = str(row.get('nexus_mod_id', ''))
                      planned = row.get('mod_version') or version
                      label = row.get('mod_filename', name)
                      url = f'https://www.nexusmods.com/{game_id}/mods/{mod_id}' if mod_id else ''
                      link = f'[{label}]({url})' if url else label

                      if not api_key or not mod_id:
                          w(f'| {link} | `{planned}` | ⚠️ skipped | — |')
                          continue

                      seen = nexus_versions(api_key, game_id, mod_id, ua)
                      if seen is None:
                          w(f'| {link} | `{planned}` | ❌ could not verify | — |')
                          continue
                      api_ok = True
                      versions_str = ', '.join(f'`{v}`' for v in seen[-5:]) or '—'
                      status = '✅ exists' if planned in seen else '🆕 new'
                      w(f'| {link} | `{planned}` | {status} | {versions_str} |')

                  w()
                  if api_key:
                      if api_ok:
                          w('> ✓ UNEX_APIKEY validated.')
                      else:
                          w('> ⚠️ UNEX_APIKEY may be invalid — all API calls failed.')
                  summary.close()
                  PYEOF
              env:
                  UPLOAD_MATRIX: ${{ needs.prepare-nexus-matrix.outputs.upload_matrix }}
                  VERSION: ${{ needs.prepare-nexus-matrix.outputs.version }}
                  NEXUS_GAME_ID: ${{ inputs.nexus_game_id || 'skyrimspecialedition' }}
                  MODE: ${{ needs.prepare-nexus-matrix.outputs.mode }}
                  UNEX_APIKEY: ${{ secrets.UNEX_APIKEY }}
                  PYTHONPATH: ${{ github.workspace }}/.github/scripts

    upload-to-nexus:
        # MATRIX MODE ONLY (per-feature fan-out to upstream pages). The AIO (aio mode) now
        # uploads via the official API (upload-aio + upload-aio-changelog below); this
        # session-cookie path is retained only for the matrix per-feature uploads.
        # Reusable workflow call: runs-on is intentionally omitted (runner is defined by the
        # called workflow). IDE schema validators may flag this as an error — it is valid syntax.
        # auto_upload=false items are pre-filtered from upload_matrix so no matrix-context
        # filtering is needed in the if condition (matrix is not available in job-level if).
        needs: [prepare-nexus-matrix, prepare-artifacts]
        if: >
            needs.prepare-nexus-matrix.outputs.dry_run != 'true' &&
            needs.prepare-nexus-matrix.outputs.has_uploads == 'true' &&
            needs.prepare-nexus-matrix.outputs.mode != 'aio'
        strategy:
            matrix: ${{ fromJson(needs.prepare-nexus-matrix.outputs.upload_matrix) }}
            fail-fast: false
        uses: alandtse/nexus-workflows/.github/workflows/upload-nexus.yml@main
        with:
            nexus_game_id: ${{ inputs.nexus_game_id || 'skyrimspecialedition' }}
            nexus_mod_id: ${{ matrix.nexus_mod_id }}
            artifact_name: nexus-upload-${{ matrix.name }}
            tag_name: ${{ needs.prepare-nexus-matrix.outputs.tag }}
            mod_version: ${{ matrix.mod_version || needs.prepare-nexus-matrix.outputs.version }}
            mod_filename: ${{ matrix.mod_filename }}
            changelog: ${{ inputs.changelog || matrix.changelog || '' }}
            # file_description anchors each .7z to the release it shipped with.
            file_description: ${{ matrix.file_description || '' }}
            check_existing: true
        secrets:
            UNEX_NEXUSMODS_SESSION_COOKIE: ${{ secrets.UNEX_NEXUSMODS_SESSION_COOKIE }}
            UNEX_APIKEY: ${{ secrets.UNEX_APIKEY }}

    upload-aio:
        # AIO MAIN file via the official Nexus API (UNEX_APIKEY) — the robust path that does
        # not depend on the session cookie (which only sets MAIN files and silently expires;
        # it dropped the v2.0.0 AIO until a manual re-run). Mirrors upstream #2405's migration
        # to the official uploader. aio mode is a single 'core' row; 7490333 is the AIO's
 # manually-minted file group on the OpenNR page. The official API cannot edit the
        # changelog tab -> upload-aio-changelog handles that best-effort below.
        needs: [prepare-nexus-matrix, prepare-artifacts]
        if: >
            needs.prepare-nexus-matrix.outputs.dry_run != 'true' &&
            needs.prepare-nexus-matrix.outputs.has_uploads == 'true' &&
            needs.prepare-nexus-matrix.outputs.mode == 'aio'
        strategy:
            matrix: ${{ fromJson(needs.prepare-nexus-matrix.outputs.upload_matrix) }}
            fail-fast: false
        uses: alandtse/nexus-workflows/.github/workflows/upload-nexus-official.yml@main
        with:
            file_group_id: "7490333"
            version: ${{ matrix.mod_version || needs.prepare-nexus-matrix.outputs.version }}
            artifact_name: nexus-upload-${{ matrix.name }}
            file_category: main
            display_name: ${{ matrix.mod_filename }}
            description: ${{ matrix.file_description || '' }}
            # Nexus already keeps a superseded version visible in the file's version
            # history, marked "old", without this. Setting it true instead hides the
            # prior AIO entirely, which is not what we want here.
            archive_existing_file: false
            # New AIO is the mod's current version and its default download.
            update_mod_version: true
            primary_mod_manager_download: true
            check_existing: true
            nexus_mod_id: ${{ matrix.nexus_mod_id }}
            nexus_game_id: ${{ inputs.nexus_game_id || 'skyrimspecialedition' }}
        secrets:
            UNEX_APIKEY: ${{ secrets.UNEX_APIKEY }}

    upload-aio-changelog:
        # Best-effort: update the Nexus mod changelog tab via the session cookie AFTER the
        # official file upload. check_existing skips the already-uploaded file; BUTR treats a
        # changelog failure as a warning (not a job failure), so a stale cookie never blocks
        # the release — only the changelog update is missed (re-run to retry).
        needs: [prepare-nexus-matrix, prepare-artifacts, upload-aio]
        if: >
            needs.prepare-nexus-matrix.outputs.dry_run != 'true' &&
            needs.prepare-nexus-matrix.outputs.has_uploads == 'true' &&
            needs.prepare-nexus-matrix.outputs.mode == 'aio'
        strategy:
            matrix: ${{ fromJson(needs.prepare-nexus-matrix.outputs.upload_matrix) }}
            fail-fast: false
        uses: alandtse/nexus-workflows/.github/workflows/upload-nexus.yml@main
        with:
            nexus_game_id: ${{ inputs.nexus_game_id || 'skyrimspecialedition' }}
            nexus_mod_id: ${{ matrix.nexus_mod_id }}
            artifact_name: nexus-upload-${{ matrix.name }}
            tag_name: ${{ needs.prepare-nexus-matrix.outputs.tag }}
            mod_version: ${{ matrix.mod_version || needs.prepare-nexus-matrix.outputs.version }}
            mod_filename: ${{ matrix.mod_filename }}
            changelog: ${{ inputs.changelog || matrix.changelog || '' }}
            file_description: ${{ matrix.file_description || '' }}
            check_existing: true
        secrets:
            UNEX_NEXUSMODS_SESSION_COOKIE: ${{ secrets.UNEX_NEXUSMODS_SESSION_COOKIE }}
            UNEX_APIKEY: ${{ secrets.UNEX_APIKEY }}

    upload-summary:
        name: Upload Summary
        runs-on: ubuntu-latest
        needs:
            [
                prepare-nexus-matrix,
                prepare-artifacts,
                upload-to-nexus,
                upload-aio,
                upload-aio-changelog,
                check-nexus-versions,
            ]
        if: always() && needs.prepare-nexus-matrix.result == 'success'
        steps:
            - name: Checkout repository
              uses: actions/checkout@v6
              with:
                  persist-credentials: false

            - name: Check upload status
              run: |
                  echo "## Nexus Upload Summary" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "**Workflow:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" >> $GITHUB_STEP_SUMMARY
                  echo "**Tag:** ${{ needs.prepare-nexus-matrix.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
                  echo "**Mode:** ${{ needs.prepare-nexus-matrix.outputs.mode }}" >> $GITHUB_STEP_SUMMARY
                  echo "**Dry run:** ${{ needs.prepare-nexus-matrix.outputs.dry_run }}" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY

            - name: Report dry-run result
              if: needs.prepare-nexus-matrix.outputs.dry_run == 'true'
              run: |
                  echo "ℹ️ Dry run — no files were uploaded to Nexus" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "**To make a real upload:**" >> $GITHUB_STEP_SUMMARY
                  echo "1. Re-run this workflow" >> $GITHUB_STEP_SUMMARY
                  echo "2. Set \`dry_run: false\` in the workflow inputs" >> $GITHUB_STEP_SUMMARY

            - name: Report upload result
              if: needs.prepare-nexus-matrix.outputs.dry_run != 'true' && needs.prepare-nexus-matrix.outputs.has_uploads == 'true'
              run: |
                  python3 << 'PYEOF'
                  import json, os
                  from nexus_changelog import nexus_versions

                  rows = json.loads(os.environ['UPLOAD_MATRIX']).get('include', [])
                  rows = [r for r in rows if not r.get('skip')]
                  version = os.environ.get('VERSION', '')
                  game_id = os.environ.get('NEXUS_GAME_ID', 'skyrimspecialedition')
                  api_key = os.environ.get('UNEX_APIKEY', '')
                  ua = 'open-shaders-ci/1.0' if os.environ.get('MODE') == 'aio' else 'community-shaders-ci/1.0'
                  summary = open(os.environ['GITHUB_STEP_SUMMARY'], 'a')

                  def w(line=''):
                      summary.write(line + '\n')

                  succeeded, failed, unknowns = [], [], []
                  rows_status = []
                  for row in rows:
                      name = row.get('name', '')
                      mod_id = str(row.get('nexus_mod_id', ''))
                      planned = row.get('mod_version') or version
                      label = row.get('mod_filename', name)
                      url = f'https://www.nexusmods.com/{game_id}/mods/{mod_id}' if mod_id else ''
                      artifact = f'nexus-upload-{name}'
                      versions = nexus_versions(api_key, game_id, mod_id, ua)
                      if versions is None:
                          status, icon = 'unknown', '❓'
                          unknowns.append(label)
                      elif planned in versions:
                          status, icon = 'uploaded', '✅'
                          succeeded.append(label)
                      else:
                          status, icon = 'failed', '❌'
                          failed.append(label)
                      rows_status.append((icon, label, planned, url, artifact, status))

                  if failed or unknowns:
                      parts = []
                      if failed:
                          parts.append(f'{len(failed)} failed')
                      if unknowns:
                          parts.append(f'{len(unknowns)} unknown')
                      w(f'⚠️ {", ".join(parts)} — {len(succeeded)} succeeded')
                  else:
                      w(f'✅ All {len(succeeded)} Nexus upload(s) completed successfully')
                  w()
                  w('| Status | Feature | Version | Nexus | Artifact |')
                  w('|--------|---------|---------|-------|----------|')
                  for icon, label, planned, url, artifact, status in rows_status:
                      art = f'`{artifact}`' if status in ('failed', 'unknown') else '—'
                      link = f'[{label}]({url})' if url else label
                      view = f'[View]({url})' if url else '—'
                      w(f'| {icon} | {link} | `{planned}` | {view} | {art} |')
                  if failed or unknowns:
                      w()
                      w('Re-run is safe: already-uploaded versions will be automatically skipped.')

                  summary.close()
                  PYEOF
              env:
                  UPLOAD_MATRIX: ${{ needs.prepare-nexus-matrix.outputs.upload_matrix }}
                  VERSION: ${{ needs.prepare-nexus-matrix.outputs.version }}
                  NEXUS_GAME_ID: ${{ inputs.nexus_game_id || 'skyrimspecialedition' }}
                  MODE: ${{ needs.prepare-nexus-matrix.outputs.mode }}
                  UNEX_APIKEY: ${{ secrets.UNEX_APIKEY }}
                  PYTHONPATH: ${{ github.workspace }}/.github/scripts
