name: "Release: Dev Rolling Prerelease"

# Builds dev-HEAD and refreshes one rolling `dev-latest` prerelease so the
# post-merge integration state is always testable without manually cutting an
# RC -- per-PR builds validate the PR branch, not the squash-merge result, and
# RCs are manual (release-semantic). Trigger/step rationale is inline below.

on:
    push:
        branches: [dev]
        # Skip docs-only / non-buildable pushes: the rolling release still
        # reflects the latest *binary* state from the prior build.
        paths:
            - "**.cpp"
            - "**.h"
            - "**.hpp"
            - "**.c"
            - "**.hlsl"
            - "**.hlsli"
            - "**.ini"
            - "src/**"
            - "include/**"
            - "features/**"
            - "package/Shaders/**"
            - "tests/**"
            - "CMakeLists.txt"
            - "CMakePresets.json"
            - "vcpkg.json"
            - "vcpkg-configuration.json"
            - ".gitmodules"
            - "extern/**"
            - "cmake/**"
            - ".github/workflows/release-dev.yaml"
            - ".github/workflows/_shared-build.yaml"
            - ".github/actions/**"
            - ".github/configs/**"
    schedule:
        # Nightly floor: the push path is a reset-debounce (below), so a fast
        # push cadence could starve it -- this guarantees >=1 build/day and
        # self-skips quiet days. 06:00 UTC clears maint-cleanup (02:00) and
        # maint-upstream-sync (Mon 08:00).
        - cron: "0 6 * * *"
    workflow_dispatch:

permissions:
    contents: read

# Newest-wins: a new push cancels any in-flight run. Safe as one group (vs
# auto-rebase's split jobs) because publishing is idempotent (ncipollo
# allowUpdates), so a cancelled publish just re-runs.
concurrency:
    group: release-dev
    cancel-in-progress: true

jobs:
    debounce:
        name: Debounce / nightly-floor gate
        runs-on: ubuntu-latest
        outputs:
            should-build: ${{ steps.gate.outputs.should-build }}
        steps:
            - name: Decide whether to build
              id: gate
              env:
                  GH_TOKEN: ${{ github.token }}
              run: |
                  set -euo pipefail
                  if [ "${{ github.event_name }}" != "schedule" ]; then
                    echo "should-build=true" >> "$GITHUB_OUTPUT"
                    exit 0
                  fi
                  # Nightly floor: only rebuild if dev actually moved, so quiet
                  # days don't burn a runner re-publishing an identical build.
                  since=$(date -u -d '25 hours ago' +%Y-%m-%dT%H:%M:%SZ)
                  count=$(gh api "repos/${{ github.repository }}/commits?sha=dev&since=${since}" --jq 'length' 2>/dev/null || echo 0)
                  if [ "${count:-0}" -gt 0 ]; then
                    echo "Nightly floor: ${count} dev commit(s) since ${since} — building."
                    echo "should-build=true" >> "$GITHUB_OUTPUT"
                  else
                    echo "Nightly floor: no dev commits since ${since} — skipping."
                    echo "should-build=false" >> "$GITHUB_OUTPUT"
                  fi

            - name: Coalesce a burst of merges
              # 10 min reset-debounce; not on the stable-release critical
              # path -- release-semantic.yaml dispatches independently.
              if: github.event_name == 'push'
              run: sleep 600

    build:
        name: Build dev-HEAD
        needs: debounce
        if: needs.debounce.outputs.should-build == 'true'
        # Trusted (own-repo) build — full suite is the dev integration gate.
        # Build github.sha for every event: the pushed commit on push, dev tip
        # on the nightly cron (dev is the default branch), or the dispatched
        # ref. The body + dev-latest tag reference the same SHA, so testers are
        # always pointed at the exact commit that was built.
        uses: ./.github/workflows/_shared-build.yaml
        with:
            ref: ${{ github.sha }}
            repository: ${{ github.repository }}
            run-cpp: true
            run-shader-validation: true
            run-shader-tests: true
            run-cpp-tests: true

    prune-stale-cache:
        name: Prune superseded CMake build-dir cache generations
        needs: build
        if: needs.build.outputs.cpp-built == 'true'
        # Trusted-only (this workflow never triggers on pull_request or
        # pull_request_target -- see the `on:` block above): deletes every
        # generation of a Windows-cmake-msvc-* cache prefix except the
        # newest, so a CMakePresets.json/extern/vcpkg.json change doesn't
        # leave a stale multi-GB generation sitting around until GitHub's
        # per-repo cache cap evicts something else instead.
        runs-on: ubuntu-latest
        permissions:
            actions: write
        steps:
            - name: Delete superseded cache generations
              env:
                  GH_TOKEN: ${{ github.token }}
              shell: bash
              run: |
                  set -euo pipefail
                  declare -A newest_id newest_time
                  while IFS=$'\t' read -r id created key; do
                      [ -z "$id" ] && continue
                      prefix=$(echo "$key" | sed -E 's/-[0-9a-f]{64}$//')
                      if [ -z "${newest_time[$prefix]:-}" ] || [[ "$created" > "${newest_time[$prefix]}" ]]; then
                          if [ -n "${newest_id[$prefix]:-}" ]; then
                              echo "Deleting superseded cache ${newest_id[$prefix]} ($prefix)"
                              gh api -X DELETE "repos/${{ github.repository }}/actions/caches/${newest_id[$prefix]}" || true
                          fi
                          newest_time[$prefix]="$created"
                          newest_id[$prefix]="$id"
                      else
                          echo "Deleting superseded cache $id ($prefix)"
                          gh api -X DELETE "repos/${{ github.repository }}/actions/caches/$id" || true
                      fi
                  done < <(gh api "repos/${{ github.repository }}/actions/caches" --paginate --jq '.actions_caches[] | select(.key | test("^Windows-cmake-msvc-")) | "\(.id)\t\(.created_at)\t\(.key)"')

    prerelease:
        name: Publish rolling dev-latest prerelease
        needs: build
        if: needs.build.outputs.cpp-built == 'true'
        runs-on: ubuntu-latest
        permissions:
            contents: write
        steps:
            # Local composite actions need the repo in the workspace.
            # fetch-depth: 0 pulls full history + tags so the changelog step can
            # diff against the last stable tag (sparse-checkout still limits the
            # working tree to the composite action).
            - name: Check out composite action
              uses: actions/checkout@v6
              with:
                  sparse-checkout: .github/actions
                  fetch-depth: 0

            - name: Generate changelog since last stable release
              id: changelog
              shell: bash
              env:
                  BUILD_SHA: ${{ github.sha }}
              run: |
                  set -euo pipefail
                  # Last stable (non-prerelease) tag; fall back to v1.5.2 since
# OpenNR has no official release yet — v1.5.2 is the
                  # inherited upstream baseline and the changelog floor.
                  BASE=$(git tag --list 'v*.*.*' --sort=-v:refname | grep -v -- '-' | head -1 || true)
                  BASE=${BASE:-v1.5.2}
                  git rev-parse -q --verify "refs/tags/${BASE}^{commit}" >/dev/null 2>&1 || BASE=v1.5.2
                  FILE="${RUNNER_TEMP}/dev-changelog.md"
                  {
                    echo "Rolling build of \`dev\` at commit ${BUILD_SHA}."
                    echo
                    echo "⚠️ Unstable integration build for testing — not a release."
                    echo "For stable builds see the latest non-prerelease."
                    echo
                    if git rev-parse -q --verify "refs/tags/${BASE}^{commit}" >/dev/null 2>&1; then
                      TOTAL=$(git rev-list --no-merges --count "${BASE}..${BUILD_SHA}")
                      echo "### Changes since ${BASE} (${TOTAL} commits)"
                      echo
                      # Cap the rendered list so a large cumulative range can't
                      # blow past GitHub's release-body size limit. `-n` (not a
                      # `| head` pipe) avoids a SIGPIPE failing the step under
                      # `set -o pipefail` when the range exceeds the cap.
                      git log --no-merges -n 400 --pretty='- %s (%h)' "${BASE}..${BUILD_SHA}"
                      if [ "${TOTAL}" -gt 400 ]; then
                        echo
                        echo "_…and $((TOTAL - 400)) more. Full list: \`git log ${BASE}..dev\`._"
                      fi
                    else
                      echo "_Base tag ${BASE} not found — changelog unavailable._"
                    fi
                  } > "${FILE}"
                  echo "body-file=${FILE}" >> "$GITHUB_OUTPUT"

            # Force-roll the rolling tag to the built commit BEFORE publishing.
            # ncipollo honors `commit:` only when it *creates* a tag — on an
            # update (allowUpdates) it leaves an existing tag pinned to its
            # original commit. Without this, dev-latest froze at the commit where
            # this workflow first published (the tag, release "target", and
            # checkout source all stuck days behind dev while only the body/asset
            # advanced). Move the ref ourselves so the release tracks dev-HEAD.
            - name: Roll dev-latest tag to the built commit
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  set -euo pipefail
                  ref="repos/${{ github.repository }}/git/refs/tags/dev-latest"
                  gh api -X PATCH "$ref" -f sha="${{ github.sha }}" -F force=true \
                    || gh api -X POST "repos/${{ github.repository }}/git/refs" \
                         -f ref="refs/tags/dev-latest" -f sha="${{ github.sha }}"

            - name: Refresh dev-latest prerelease
              uses: ./.github/actions/publish-release
              with:
                  # GITHUB_TOKEN (not a PAT) so this can't trigger release-build.
                  token: ${{ secrets.GITHUB_TOKEN }}
                  # Non-semver tag: dodges maint-cleanup and the dev branch name.
                  tag: dev-latest
                  # First-publish only (ncipollo ignores this on update); the
                  # Roll step above keeps the existing tag tracking dev-HEAD.
                  commit: ${{ github.sha }}
 name: "OpenNR (dev) — latest build ${{ needs.build.outputs.version }}"
                  prerelease: "true"
                  artifacts: "${{ github.workspace }}/dist/CommunityShaders_AIO-*.7z"
                  body-file: ${{ steps.changelog.outputs.body-file }}
